<UserControl x:Class="Stock.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" 
             Background="{StaticResource DefaultBackground}">
    
    <Grid>
        <StackPanel Orientation="Vertical" Width="250" VerticalAlignment="Center">
            <TextBlock Text="Логин:" Style="{StaticResource DialogTextBlockStyle}" />
            <TextBox x:Name="LoginTextBox" VerticalContentAlignment="Center" Height="32" Margin="0,0,0,8" Style="{StaticResource TextBoxStyle}" />
            <TextBlock Text="Пароль:" Style="{StaticResource DialogTextBlockStyle}" />
            <PasswordBox x:Name="PasswordBox" VerticalContentAlignment="Center" Height="32" Margin="0,0,0,8" Style="{StaticResource PasswordBoxStyle}"/>
            <Button Content="Вход" Click="ButtonBase_OnClick" IsDefault="True" Height="32" Style="{StaticResource ButtonSuccessStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
